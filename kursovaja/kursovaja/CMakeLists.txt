# CMakeList.txt : CMake project for kursovaja, include source and define
# project specific logic here.
#

# Add source to this project's executable.
add_executable (kursovaja "kursovaja.cpp" "kursovaja.h")

# TODO: Add tests and install targets if needed.
